# ============================================================
# 灯笼 · 多维轴知识库 —— Git 忽略规则
# 原则：代码与配置入库；个人数据、凭据、运行时产物不入库。
# ============================================================

# ---- 凭据与密钥（含敏感信息，绝不入库）----
.env
llm_config.json

# ---- 本地知识库数据（个人数据，绝不入库）----
# 真实 lantern.db / articles 由 .gitignore 排除；仓库以「空库 + seed_demo.py」
# 形式发布，克隆后运行 `python seed_demo.py` 生成合成演示数据。
lantern.db
lantern.db-shm
lantern.db-wal
lantern.db.backup*
llm_cache.db
articles/*.md
articles/**/*.md

# ---- 全局附件（图片/视频，个人数据，绝不入库）----
attachments/

# ---- 本地主尺定义（user 本地校准，不入库）----
main_scale_definition.json

# ---- 运行时 / 日志 ----
server.log
__pycache__/
*.pyc
*.pyo

# ---- 本地备份与清理暂存（删除文件的中转区，非仓库内容）----
backups/
_deleted_*/

# ---- 本地数据库副本（个人数据，绝不入库）----
lantern_caliper.db
lantern_caliper.db-shm
lantern_caliper.db-wal

# ---- 临时诊断 / 探针脚本（下划线前缀，非项目内容）----
_tmp_*.py
_probe_*.py
*.db.backup*

# ---- 本地数据库安全副本（排障用，非仓库内容，绝不入库）----
lantern.db.bak*
lantern.db.safe*

# ---- 操作系统 / 编辑器杂项 ----
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ---- Skill 运行时状态（由 AI 驱动生成，非仓库内容）----
skills/*/state/*.json
skills/*/scripts/__pycache__/

# ---- 本地离线模型：现已通过 Git LFS 纳入仓库（见 .gitattributes），克隆即随附，无需手动下载 ----
# 仅大体积权重（*.safetensors/*.bin）走 LFS；config/tokenizer 等小 json 仍作普通文本入库。

# ---- 临时备份快照（如 schema.json.bak-*）----
*.bak

# ---- 临时测试/副本（_remtest 下为 db 副本等一次性产物，绝不入库）----
_remtest/
