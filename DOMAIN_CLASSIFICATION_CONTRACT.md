# 领域划分分类契约（Domain Classification Contract）

> 本文件是 lantern-caliper 知识库「领域种类划分」的**唯一事实来源（single source of truth）**。
> 它被 `llm.py:_main_system()` 注入为大模型 domain-classifier 的 system prompt 基底，
> 也被 `_is_broad_field()` / `consolidate_domains()` 的本地把关逻辑反向印证。
> 修改此处即修改模型的「领域把关」行为。

## 0. 核心隐喻：两把尺，各管一维

| 尺 | 维度 | 回答的问题 | 谁来判 |
|----|------|-----------|--------|
| 主尺（main） | 领域带（band） | 它大致属于哪片知识大陆？ | 模型归纳 + 本地守门 |
| 游标（vernier） | 严密度（depth） | 在这片大陆里，它的论证多形式化？ | 模型评估论证骨架（不看内容） |

**两尺独立、互不替代**：主尺只看主题词，绝不评估论证是否严密；游标只看论证骨架，绝不猜测学科。
任何让两尺串味的设计都是错的（已在 prompt 里用「那是另一把尺子的事，与你无关」硬性隔离）。

## 1. 领域是「涌现」的，不是预设清单

- 内容进来，领域出现；内容删光，领域消失（`list_domains()` 实时从 readings 聚类）。
- **不要**去套一个固定的「学科树」或「分类法」。系统不维护权威领域字典。
- 预设的只有 4 条**主干学科带**（人文 / 社会科学 / 自然科学 / 形式科学），作为最后的兜底锚，不是分类目标。

## 2. 划对的唯一标准：够宽、能容纳多条不同内容

一个候选领域名「成立」，当且仅当它满足：

- 它是「学科 / 主题领域」这种**够宽**的范畴（如「信息检索」「认知科学」「组织行为」）；
- 它的**代表词**（高频实义词）在待分类内容里**至少命中 ≥ 2 个**，且覆盖度够宽；
- 代表词命中不足 2 个 → 该内容只聊某项具体技术/实体 → **不立领域**，退回主干带，具体技术留给标签（tags）。

### 2.1 划错只有两类，都要防

| 错误类型 | 表现 | 正确做法 |
|---------|------|---------|
| **过碎**（最常见） | 把某项具体技术/方法/模型/实体立成领域（如「RAG」「重叠窗口」「梯度下降」「React Hooks」） | 降级为标签，归入其上位学科领域 |
| **过粗** | 把主题仅名义相关、代表词重叠度低的内容硬塞进同一主干带 | 另立更贴的细领域，而非强塞；让代表词 Jaccard ≥ 0.5 的近义领域自动合并回上位 |

### 2.2 命名禁忌

- 禁用具体技术/方法/模型/实体当领域名（RAG、向量、梯度下降、某框架…）。
- 禁用空名（「综合」「其他」「杂项」「未分类」）。
- 领域名长度 2–8 个汉字。

## 3. 模型自滤流程（每次分类照此执行）

1. 看 `domains` 列表里每个领域的「代表词」与「中心位置」。
2. 数待分类文本里命中了哪个领域的几个代表词。
3. **命中 ≥ 2 个** → 归入该领域（position 靠其中心 ±15 内）。
4. **命中 < 2 个** → 若文本主题明显属于某个新学科，则**自创**一个够宽的领域名（2–8 汉字）；
   若文本只讲一项技术，则给它的**上位学科领域**名（如讲 RAG → 归「信息检索」）。
5. **绝不**因文本"提到了"某技术就立该技术为领域。

## 4. 自纠机制（consolidate_domains）

当库里出现两个领域，其代表词集合 Jaccard 相似度 ≥ 0.5，说明本应同属一个领域却各立山头
（图谱被近义碎片切碎）。系统自动把较小的并入较大的，被并名字进入标签保留信息，不丢。
主干学科带不参与合并。

## 5. 守门红线（本地 _is_broad_field 强制）

即便模型吐出了领域名，只要它属于以下任一，本地一律拒绝并退回主干带：
- 空 / 单字；
- 整词就是某项技术/实体（rag / 向量 / 重叠窗口…）；
- 含技术构造词（语义分块 / 梯度下降…）；
- 又短又只出现一两次（偏术语/实体）。

**模型被判退回时，原因会写入 feedback_inbox（自我对抗审查收件箱），形成闭环：**
模型在后续分类中可参考历史退回记录，逐步收敛到正确边界。

## 6. 一句话精髓（给模型的最短指令）

> 领域是内容长出来的学科大陆，不是技术碎片。够宽、代表词命中 ≥ 2 才立；
> 否则退主干带、技术进标签。两尺各管一维，互不串味。

## 7. L0 摘要字段约束（第 1 层固化，为第 2 层留形）

领域把关解决「这条知识在哪个学科大陆」，而 L0 摘要解决「这条知识一句话是什么」——
两者都是第 1 层（储存优化）应固化成型的结构化字段，供未来第 2 层（面向 agent 调用的
检索 / 分层加载）直接消费，无需现在实现 L0/L1/L2 下钻机制。

- **`summary` = L0 抽象层**：一句话概括本条知识的核心，约束为 **≤ 80 汉字（约 100 token）**，
  纯陈述、不含 markdown、不含「本文/该文」等自指。
- 由 `summarize()` 生成（真实模型优先，不可用时 `local_summarize` 取首句兜底），入库即落库，
  不允许空 summary。
- `axis_domain`（学科域）+ `band`（主尺带，来自 readings.label）+ `tags`（关键词索引）与
  `summary` 共同构成 L0 视图，由 `store.item_l0(item_id)` 聚合返回（**不含正文 content**）。
- 第 2 层取 L0 时直接消费 `item_l0()`，按需再下钻 L1（概览）/ L2（原文）——那是第 2 层的事，
  当前阶段只确保 L0 字段齐全、规范。

